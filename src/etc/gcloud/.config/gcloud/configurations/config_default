# Warning: default configuration file generated by SysDen64. No further changes will be managed.
# To update this file:
#   - ensure that the right profile is active
#   - edit the file in its original location. Changes are isolated to the active profile.
# Version: 1.0.0

[core]
console_log_format = detailed
default_format = json
disable_color = false
disable_usage_reporting = true
show_structured_logs = always
verbosity = info
